---
interface Props {
  label: string;
  body: string;
}
const { label, body } = Astro.props;
---

<div class="cc-card">
  <div class="cc-card__bar"></div>
  <div class="cc-card__body">
    <span class="cc-card__label">{label}</span>
    <p class="cc-card__text">{body}</p>
  </div>
</div>
